import 'package:flutter/cupertino.dart';

abstract class FirebaseDimensions {
  static const imageHeightCoefficient = 0.45;
  static const outputBorder = 20.0;
  static const emailTopPadding = 30.0;
  static const passwordTopPadding = 20.0;
  static const loginTitleFontSize = 25.0;
  static const loginButtonFontSize = 18.0;
  static const contentBasePadding = EdgeInsets.all(8.0);
}
